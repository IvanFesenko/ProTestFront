import styled from 'styled-components';

export const Main = styled.main`
  padding-top: calc((100vh - 381px) / 2);
  text-align: center;
`;

export const Image = styled.img`
  display: inline-block;
  margin-bottom: 20px;
`;

export const Title = styled.h1`
  font-weight: 700;
  font-size: 24px;
`;
