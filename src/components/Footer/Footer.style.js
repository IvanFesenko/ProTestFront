import styled from 'styled-components';

import { secondaryBackground } from 'assets/colors'

export default styled.footer`
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 10px 18px;
  background-color: ${secondaryBackground};
`;
